package com.example.contents;

import com.example.contents.model.Content;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest
class RecursiveContentApiApplicationTests {

//	@Test
//	void contextLoads() {
//        Content content = new Content();
//        Long expectedId = 42L;
//        String expectedFile = "https://example.com/pic.png";
//        String expectedText = "Sample text block";
//
//        content.setId(expectedId);
//        content.setFile(expectedFile);
//        content.setTextBlock(expectedText);
//
//
//        assertThat(content.getId()).isEqualTo(expectedId);
//        assertThat(content.getFile()).isEqualTo(expectedFile);
//        assertThat(content.getTextBlock()).isEqualTo(expectedText);
//
//	}

}
